#include <stdio.h>

int main() {
    int a = 10;
    int b = 25;

    if (a > b) {
        printf("The larger value is: %d\n", a);
    } else {
        printf("The larger value is: %d\n", b);
    }

    return 0;
}
